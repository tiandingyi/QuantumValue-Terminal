# Frontend Template Staging Area

Place the raw dashboard template source files here before migrating them into `apps/web`.

Recommended contents:
- original HTML files
- copied CSS or Tailwind reference assets
- screenshots of the intended UI
- vendor JS files that help explain template behavior

Keep this folder as a reference source. Production application code should live in `apps/web`.
