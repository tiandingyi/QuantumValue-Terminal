{
  "name": "quantumvalue-terminal",
  "private": true,
  "packageManager": "pnpm@10.6.5",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "dev:web": "pnpm --filter web dev"
  },
  "devDependencies": {
    "turbo": "^2.4.4"
  }
}
